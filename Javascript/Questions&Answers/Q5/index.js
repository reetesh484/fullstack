// 05. Declare a varibale and assign string value to it and then split it into an array using split() method

let sentence = "Javascript is quite a tricky language.";

let words = [];

//In the parenthesis we need to specify the splitting operator
words = sentence.split(" ");

console.log(words);
